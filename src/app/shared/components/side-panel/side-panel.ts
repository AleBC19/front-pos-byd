import { Component } from '@angular/core';

// TODO: panel lateral (drawer) con backdrop y ng-content
@Component({
  selector: 'app-side-panel',
  template: `<p>side-panel</p>`,
})
export class SidePanel {}
