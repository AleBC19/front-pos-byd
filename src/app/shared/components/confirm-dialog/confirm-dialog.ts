import { Component } from '@angular/core';

// TODO: modal de confirmación (title, message, confirmed output)
@Component({
  selector: 'app-confirm-dialog',
  template: `<p>confirm-dialog</p>`,
})
export class ConfirmDialog {}
