import { Component } from '@angular/core';

// TODO: tabla genérica con paginación (columns, data, pageChange, rowClick)
@Component({
  selector: 'app-data-table',
  template: `<p>data-table</p>`,
})
export class DataTable {}
