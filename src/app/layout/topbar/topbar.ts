import { Component } from '@angular/core';

// Barra superior: estado de sesión (caja activa, usuario, notificaciones, fecha/hora).
// TODO: datos reales de sesión/caja.
@Component({
  selector: 'app-topbar',
  templateUrl: './topbar.html',
})
export class Topbar {}
